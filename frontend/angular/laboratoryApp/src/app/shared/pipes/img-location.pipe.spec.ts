import { ImgLocationPipe } from './img-location.pipe';

describe('ImgLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
